using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class spotTrident : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

  /*  void OnTriggerEnter2D(Collider2D col)
    {
        if (col.tag == "Trident")
        {
            moveFish.run=true;
            Invoke("Stop", 2.0f);
        }
    }
    void Stop()
    {
        moveFish.run = false;
    }*/
}
